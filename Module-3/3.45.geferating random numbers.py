
"""
Q1:How will you set the starting value in generating random numbers?

ANS:

==> The seed() method is used to initialize the random nummber generator.

==> The random number generator needs a number to start with(a seed vvalue),to be able to generate a random number.

==> By default the random the random number generator uses the current system time.

"""